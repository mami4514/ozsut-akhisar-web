info:
  name: Create Job Application
  type: http
  seq: 1

http:
  method: PATCH
  url: http://127.0.0.1:8000/api/job-applications/1/status
  body:
    type: json
    data: |-
      {
        "status": "interview",
        "admin_note": "Telefon görüşmesi olumlu geçti."
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
